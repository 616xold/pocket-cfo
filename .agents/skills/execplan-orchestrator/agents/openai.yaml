interface:
  display_name: "ExecPlan Orchestrator"
  short_description: "Create or update a living execution plan before major changes."
  brand_color: "#1D4ED8"
  default_prompt: "Create or update an ExecPlan for the current milestone before implementing it."
policy:
  allow_implicit_invocation: true
